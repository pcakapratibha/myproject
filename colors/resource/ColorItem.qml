import QtQuick 2.1
import QtMultimedia 5.0
import "findit.js" as Activity
import "qrc:/gcompris/src/core"
import GCompris 1.0
import "colors.js" as Acto

/*Text {
    id: item
    property Item main
    property Item bar
    property string question
    property string answer
    Column{
        Repeater {
            model: 6
            Text { id:rt
                text: "I'm item " + index
                    font.pixelSize:20  }
        }
    }



    MouseArea {
        anchors.fill: parent
        onClicked: {
            if(rt.text === Activity.getCurrentAnswer()) {

                particles.emitter.burst(40)
                Activity.nextQuestion()
                }
             else {
                Activity.lost()
            }
        }
    }

    ParticleSystemStar {
        id: particles
        clip: false
    }
 ColorItem {
                 question: text
                 font.pointSize: 24
                 font.weight: Font.DemiBold
                 style: Text.Outline
                 styleColor: "black"
                 color: "white"
                 anchors{
                     verticalCenter: parent.verticalCenter
                     horizontalCenter: parent.horizontalCenter
                     }
                 Column{
                     Repeater {
                         model: 6
                         Text { text: "I'm item " + index
                                 font.pixelSize:20  }*/
                     //}
                // }


             //}

